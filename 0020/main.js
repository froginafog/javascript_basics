function main()
{
    var a = [10, 20, 30, 40, 50];
    var num_elements = a.length;  //number of elements in "a"
    
    document.getElementById("p1").innerHTML = "There are " + num_elements + " elements in the array [" + a + "].";
}

main();

